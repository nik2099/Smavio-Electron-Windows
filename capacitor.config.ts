import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smavio.player',
  appName: 'smavio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
